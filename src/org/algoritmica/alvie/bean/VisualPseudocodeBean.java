package org.algoritmica.alvie.bean;

/*
 * This the visual pseudo-code bean which does not add any functionality to
 * the visual malleable data structure bean, since all its elements have
 * a rectangular shape (even though, differently from arrays, the border of 
 * the rectangle will not be drawn).
 */
public class VisualPseudocodeBean extends VisualArrayBean {
}
